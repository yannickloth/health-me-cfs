// TODO: Convert TikZ to CeTZ
// Original: figures/fig-load-bearing-locks.tex
#figure(
  rect(width: 100%, height: 5cm, fill: luma(240), stroke: luma(200))[
    #align(center + horizon)[_Figure placeholder: fig-load-bearing-locks_]
  ],
  caption: [fig-load-bearing-locks (TikZ conversion pending)],
) <fig:load-bearing-locks>
