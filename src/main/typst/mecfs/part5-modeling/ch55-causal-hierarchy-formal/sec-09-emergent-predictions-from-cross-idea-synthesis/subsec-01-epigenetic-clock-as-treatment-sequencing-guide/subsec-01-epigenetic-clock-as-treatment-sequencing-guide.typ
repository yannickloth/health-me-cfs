#import "../../../../shared/environments.typ": *

=== Epigenetic Clock as Treatment Sequencing Guide

The epigenetic clock (Section @sec:epigenetic-clock) and the lock-removal sequence dependence (Section @sec:lock-sequence) combine to produce a practical clinical algorithm: measure methylation deviation $||bold(cal(M)) - bold(cal(M))^"baseline"||$ and the locus-class balance (ProB vs ProA vs gene-region) before treatment to determine the optimal intervention sequence.

    - *Low deviation + loss-dominant ($||bold(cal(M))_"ProB" - bold(cal(M))_"ProB"^"baseline"|| > ||bold(cal(M))_"ProA" - bold(cal(M))_"ProA"^"baseline"||$, early disease):* Epigenetic consolidation is underway at ProB repeats. Methyl-donor support (SAMe, methyl-folate) to halt further erosion, combined with root cause targeting. The window of opportunity is closing.
    - *High deviation + mixed pattern (late disease):* Deep consolidation with significant deviation in both directions. Methyl-donor support plus energy restoration must be sustained to enable passive demethylation at hypermethylated loci (months) and active remethylation at ProB repeats (months to years). Even aggressive root cause removal will fail without concurrent epigenetic normalization and energy restoration.

This algorithm transforms the epigenetic clock from a diagnostic tool into a _treatment decision instrument_.

